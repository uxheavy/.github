# UXHeavy repository policy

Canonical, dependency-free enforcement shared by UXHeavy repositories.

Repositories call the reusable workflow and keep only their own license and
upstream-owner configuration in `.github/uxheavy-copyright.json`.

The reusable workflow exposes one stable status check: `Copyright attribution`.
Organization rulesets can require that check on protected default branches.
